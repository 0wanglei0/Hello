# 29个你必须知道的Linux命令

> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
> *1.ls
>> 列举出当前工作目录的内容（文件或文件夹）
